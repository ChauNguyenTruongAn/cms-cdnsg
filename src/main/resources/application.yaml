spring:
  application:
    name: warehouse-cdnsg

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/cdnsg_db_v2}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
  
server:
  port: 8080

app:
  cors:
    origin: ${CORS_ORIGINS:http://localhost:5173}